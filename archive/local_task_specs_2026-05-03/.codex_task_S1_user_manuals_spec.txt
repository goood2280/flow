flow 앱(/mnt/d/TEST_Making_Video/semi_all/flow) 사용자 매뉴얼 + 가이드 문서 자동 생성. spark codex 가 메인 codex chain (task #15~18) 과 병렬 진행. 충돌 회피 위해 docs/ 신규 파일만 생성.

[배경 — 사용자 직언]
"작업 길어지거나 하면 병렬적으로 할 수 있는 일들은 gpt5.3-codex-spark 도 써줘."

선택 이유: docs 작성은 메인 chain 의 코드 변경과 충돌 X (신규 파일만), 사용자 매뉴얼은 작업 완료와 별개로 가치, 메인 codex 의 진행 결과를 spark 가 read 해서 정확히 문서화 가능.

[제약 — 매우 중요]
- **충돌 회피**: 다음 파일들은 절대 수정하지 마. 메인 codex 가 진행 중.
  * backend/routers/informs.py / splittable.py / dashboard.py / llm.py / files.py
  * frontend/src/pages/My_Inform.jsx / My_Dashboard.jsx / My_Diagnosis.jsx / My_FileBrowser.jsx
  * tests/inform/ / tests/agent/
  * setup.py / _build_setup.py
- **신규 파일만 생성**: docs/ 디렉토리 안 신규 markdown / PDF 파일.
- admin_settings.json 절대 X. git add/commit/push 절대 X.

[원하는 결과 — 6 매뉴얼 + 1 인덱스]

1) docs/user_guide_inform.md
   - 인폼 페이지 사용법 (task #12~14 결과 반영).
   - 섹션:
     a. 개요 — 인폼이란 (랏 관리 부서 → 모듈 인폼).
     b. 탭 구조 — 인폼 / 매트릭스 / 로그 (3탭).
     c. 인폼 탭 — 표 layout (fab_lot/root_lot/제품/제목/상태/작성자/시간/카운트/모듈).
     d. 신규 인폼 등록 — wizard 5단계 (제품/Lot/모듈/본문/첨부).
     e. 첨부 — KNOB 직접 / 기존 커스텀 세트 / 새 세트 만들기. default 0 선택.
     f. 매트릭스 — 랏×모듈, 셀 클릭, lot 검색.
     g. 로그 — audit 추적.
     h. 권한 — 모듈별 / 페이지 admin / 작성자 / 일반 user.
     i. 메일 — throttle, 모듈 wise.
     j. 자주 묻는 질문 (10+ FAQ).
   - 분량 200~300 줄, 한국어, 마크다운.

2) docs/user_guide_splittable.md
   - SplitTable 사용법.
   - 섹션:
     a. 개요 — SplitTable 이란 (랏 단위 plan/notes).
     b. 제품 선택 — ML_TABLE_*.
     c. Lot 검색 — root_lot_id / fab_lot_id.
     d. 컬럼 선택 — wide / long / custom.
     e. KNOB / MASK / FAB / INLINE / VM / ET 5소스.
     f. 커스텀 세트 — paste-sets / customs / saved views.
     g. 노트 / plan 저장 — notes/save.
     h. override / debug.
     i. 25 wafer 풀 표시.
     j. FAQ 10+.
   - 분량 200~300 줄.

3) docs/user_guide_filebrowser.md
   - 파일탐색기 사용법.
   - 섹션:
     a. 개요 — 사내 파일 시스템 / hive parquet.
     b. 트리 / 목록 / 검색.
     c. SQL 미리보기 — apply_sql_like.
     d. ML_TABLE 미리보기.
     e. hive view / parquet preview.
     f. 업로드 / 이동 / 삭제 (admin).
     g. S3 sync.
     h. FAQ 10+.

4) docs/user_guide_dashboard.md
   - 대시보드 사용법 (task #15 결과 포함).
   - 섹션:
     a. 개요 — chart 시스템.
     b. 차트 종류 — scatter/line/bar/pie/donut/heatmap/treemap/wafer_map/binning/box/area/pareto/combo/step_knob_binning/table.
     c. 멀티 Y / 시리즈 색.
     d. 인폼 차트 12종 — task #15 결과 (모듈별/일별/제품/상태/매트릭스 미니/응답시간/처리율/작성자top10/활동heatmap/모듈x제품/첨부메일/오래된미해결).
     e. 데이터 source — DB/file/inform.
     f. 공개 범위 / 그룹.
     g. FAQ 10+.

5) docs/user_guide_agent.md
   - 에이전트 사용법 (task #16~18 결과 포함).
   - 섹션:
     a. 개요 — 사용자 권한 안에서 모든 작업 자연어로 호출.
     b. 카테고리 — 페르소나 / 시나리오 / Knowledge / Audit / Rules / 등.
     c. 시나리오 카탈로그 30+ — 파일탐색기/SplitTable/인폼.
     d. 멀티턴 대화 — clarification, 컨텍스트 보존, entities pill.
     e. Suggestions — chip / quick action / navigate.
     f. follow_up_inputs — slot filling.
     g. Thread — 영속화, 재방문 시 복구.
     h. Streaming — 점진적 응답.
     i. 권한 — RBAC (사용자 권한 그대로).
     j. FAQ 10+.

6) docs/user_guide_admin.md
   - admin / 페이지 관리자 가이드.
   - 권한 매트릭스 / 카탈로그 관리 / 모듈 순서 / 활동 로그 / 메일 그룹 / 백업.

7) docs/user_guides_index.md
   - 위 6개 가이드 인덱스 + 각 가이드 1줄 요약 + 직접 링크.
   - 사용자가 가장 먼저 봐야 할 곳 (Quick Start 5분).

[작업 순서]

A) 매뉴얼 마크다운 6개 + 인덱스 1개 생성.
B) 각 마크다운 끝에 "최종 검증: code reference 와 현재 동작 일치 여부 확인 필요" 메모 (메인 chain 끝나면 보강).
C) docs/ 디렉토리에 위 7개 파일 commit (단, git add/commit/push 절대 X — 디스크에만 저장).

[절대 하지 말 것]
- 위 [제약] 의 파일들 수정.
- admin_settings.json 수정 / 출력.
- git add/commit/push.
- 메인 codex 가 작업 중인 spec (.codex_task_15~18) read 해서 추측 — spec 자체는 read 해도 OK 지만 코드 적용 X.

[완료 기준]
- docs/ 안에 user_guide_*.md 6개 + user_guides_index.md 1개 생성.
- 각 200~300 줄, 한국어, 마크다운.
- code reference 정확 (현재 코드 구조 기준).
- 마지막에 'DISCORD_SUMMARY:' 한국어 3~5줄 요약 (어떤 매뉴얼 만들었는지).

[CHAIN — task S1 완료 후 자동 이어가기 (spark 한도 풍부)]
이 task S1 가 끝났으면 (DISCORD_SUMMARY 출력 직전 또는 직후) 메인 codex 의 chain 을 spark 가 이어받아 진행합니다:
1. `.codex_task_16_agent_full_capability_spec.txt` 파일을 read 도구로 전체 read.
2. spec 안 모든 요구사항 (에이전트 페이지 capability list 제거 + RBAC 기반 권한 확장 + FB/SplitTable/인폼 tool registry) 단계별로 빠짐없이 구현.
3. task #16 의 회귀/빌드/테스트 까지 마무리.
4. task #16 의 'DISCORD_SUMMARY:' 한국어 3~5줄도 출력 (task S1 요약과는 별도 블록).
5. task #16 spec 끝의 [CHAIN] 명령에 따라 task #17 → task #18 까지 자동 chain.
같은 동일 제약 (git add/commit/push X, admin_settings.json X, 내용 출력 X) 모두 후속 task 들에도 적용.

[중요 — 메인 codex 와의 충돌 회피]
메인 codex (codex-main 세션) 가 task #15 (dashboard.py / informs.py / My_Dashboard.jsx 변경) 진행 중일 수 있음.
spark 가 task #16 진입 전 다음 확인:
- `git status --short` 로 unstaged 변경 보고 있다면 메인이 아직 작업 중일 가능성.
- 변경 중인 파일과 충돌하는 부분은 메인 작업 끝날 때까지 대기 (sleep 60 후 재확인).
- task #16 은 주로 llm.py + My_Diagnosis.jsx 변경 — task #15 와 겹치지 않음. 안전.
- task #17 #18 도 llm.py + My_Diagnosis.jsx + tests/agent/ — task #15 와 겹치지 않음.
