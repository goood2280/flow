[중요 안내 — 메인 codex 는 진행 X]
이 task 는 사용자 codex weekly limit 보호 차원에서 spark codex (model=gpt-5.3-codex-spark) 만 처리합니다.
메인 codex (model=gpt-5.5) 가 이 spec 을 read 했다면 즉시 종료. spark 만 chain 으로 진입.

flow 앱(/mnt/d/TEST_Making_Video/semi_all/flow) 인폼 wizard / detail / 메일 미리보기 4가지 사용자 직언 fix. task #14 후속 polish.

[배경 — 사용자 직언 9차]
사용자가 화면 스크린샷 3장으로 보고:

1. 저장된 커스텀 세트(test1 등)를 wizard 에서 첨부하면 detail 에는 set 이름만 (cols 1·rows 0) 나오고 **SplitTable preview 가 안 보임**.
2. SplitTable 좌상단 인덱스 영역에 "root_lot_id A1005 / lot_id A1005A.2, A1005A.3, A1005B.2, ..." 처럼 **값들이 다 나열됨**. 사용자: "row 인덱스에는 **컬럼 이름만** (`root_lot_id`, `lot_id`) 표시되어야 함. 값 종류가 다 나오는 게 아니라."
3. 메일 미리보기:
   - "KNOB 강조" panel 필요 X (제거)
   - "자동 수신자" 대신 **그룹 선택 + 개별 수신자 선택** 가능하게
   - 본문에는 인사말 + 제품 + 랏 + **note(작성자가 입력한 내용)** 만 (KNOB 강조 prefix 등 X)
   - 제품/랏 나오는 건 좋음
   - 미리보기 패널이 좁아서 SplitTable 옹색 — **풀화면 모드일 때 SplitTable 잘 들어가게**

[원하는 결과 — 4가지 fix]

1) 저장된 커스텀 세트 첨부 시 SplitTable preview 표시.
   - 원인 추정: wizard 가 attached_sets 에 {id, source} 만 보내고, server 가 set 의 실제 컬럼/wafer 데이터를 resolve 안 함 → detail 에 SplitTable HTML 미생성.
   - 수정:
     a. 신규 헬퍼 `_resolve_attached_set(set_id, source, product, root_lot)` — paste-sets / customs / saved 에서 실제 컬럼 list 와 (lot, wafer) data 가져옴.
     b. POST /api/informs 또는 inform save 시점에 attached_sets resolve → entry.attachments 에 {id, source, name, columns[], wafers[], html_snapshot} 저장.
     c. detail 응답에 attachments 의 html_snapshot 포함.
     d. 카드/detail 의 첨부 stack — set 이름 아래에 "📋 SplitTable · {product} @ {root_lot} · {scope}" 헤더 + html_snapshot 렌더.
     e. cols 0 / rows 0 일 때만 "데이터 없음" 폴백 표시.
   - tests/inform/test_attached_set_resolve.py 신규: paste-sets 기반 첨부 → detail 응답에 columns·wafers·html 포함.

2) SplitTable 좌측 인덱스 표기 정정.
   - 현재 좌상단 cell 에 "root_lot_id A1005 / lot_id A1005A.2, A1005A.3, A1005B.2, A1005C.1, A1005C.2" 처럼 컬럼 이름 + 값 모두 나옴.
   - 변경: 좌측 인덱스 영역에는 **컬럼 이름만** ("root_lot_id", "lot_id") 한 줄씩.
   - 값들은 가로 헤더 행에 이미 나오므로 좌상단에는 중복 X.
   - 단 SplitTable 전체 wafer 가로 헤더가 root_lot/lot_id row 에 있으므로 좌상단 = 단순히 row 라벨 ("root_lot_id" / "lot_id" / "parameter") 만.
   - frontend SplitTable 렌더 함수 (HTML/snapshot 양쪽) 수정 — 좌상단 cell 의 텍스트 빌더에서 값 join 부분 제거.
   - SplitTable 의 다른 곳 (실제 SplitTable 페이지) 도 동일하게 적용 — 일관성.
   - tests/splittable/test_split_index_label.py 신규: 좌상단 cell 에 값 미포함 검증.

3) 메일 미리보기 재구성.

   3a. KNOB 강조 panel 제거.
       - wizard 의 메일 미리보기 좌측 panel 의 "KNOB 강조 설정 없음" 영역 (또는 그 배경 로직) 모두 제거.
       - related state / props / endpoint 정리.

   3b. 자동 수신자 → 그룹 선택 + 개별 수신자 선택.
       - "자동 수신자 / 모듈 수신자 없음" 영역 제거.
       - 신규 영역 "📨 수신자":
         * **그룹 선택 multi-select** — 메일 그룹 카탈로그 (memory v8.7.7 의 공용 메일그룹 CRUD 활용). 체크박스 또는 chip.
         * **개별 수신자 multi-select** — 사용자 list (admin_settings 에서 RBAC 가능 user) 검색 + 체크박스 또는 chip.
         * 선택된 수신자 = 그룹의 멤버 union + 개별 선택 union (중복 제거).
         * 선택된 수신자 미리보기: "→ 발송 대상 X명: [name1, name2, ...]" 표시.
       - 기본값: 비어있음 (사용자 명시 선택 필수).
       - POST /api/informs payload 에 recipients = {groups: [...], users: [...]} 전달.
       - server 가 발송 시 union resolve.

   3c. 본문 = 인사말 + 제품 + 랏 + note(내용) 만.
       - 현재 본문에 "안녕하세요. Spacer팀에 다음과 같이 plan 적용 인폼을 등록합니다. 제품: PRODA Lot: A1005 asdasda" 같이 노출 — 이 형태 OK.
       - KNOB 강조 prefix / 첨부 자동 본문 / reason "PEMS" 같은 메타 prefix 모두 제거.
       - 본문 = 인사말 한 줄 + "제품: {prod}" + "Lot: {root_lot}" + 빈 줄 + note(사용자 입력 내용).
       - 그 아래 SplitTable embed (3d 참조).
       - 메일 빌더 (informs.py 의 mail_compose 또는 비슷) 수정.

   3d. SplitTable 풀화면 폭 cleanup (메일 미리보기 풀화면 모드).
       - 메일 미리보기 패널이 폭 좁을 때 SplitTable HTML 이 옹색 — 풀화면 모드에서 폭 활용.
       - 미리보기 컨테이너의 max-width 제거 또는 풀폭 (100vw - sidebar / 패널 약 80%).
       - SplitTable HTML 의 cell font 11px / padding 2px 3px / table-layout fixed 적용 (task #14 v3.1 의 4 항목 재검증).
       - 풀화면 모드 토글 버튼 신설 — "🗖 미리보기 확대" → 미리보기 area 가 화면 80% 차지.
       - 25 wafer 모두 한 줄에 표시.
       - 메일 본문 자체도 풀폭 — note 텍스트 + SplitTable 위아래로 자연스럽게.

[코드 reference]
- frontend/src/pages/My_Inform.jsx — wizard, detail, 메일 미리보기
- backend/routers/informs.py — POST /api/informs (attached_sets resolve, recipients, mail_compose)
- backend/routers/splittable.py — paste-sets, customs, SplitTable HTML 빌더
- frontend/src/pages/SplitTable/* — SplitTable 렌더 함수 (좌상단 cell)
- backend/routers/groups.py 또는 mail_groups — 메일 그룹 카탈로그 (memory v8.7.7)

[작업 순서]

A) Backend
   1. _resolve_attached_set 헬퍼 신규.
   2. POST /api/informs 가 attached_sets 받으면 resolve → entry.attachments 에 columns/wafers/html_snapshot 저장.
   3. detail 응답 (/api/informs?inform_id 또는 by-lot) 에 attachments 의 html_snapshot 포함.
   4. recipients = {groups, users} payload 받아 발송 대상 union resolve.
   5. mail_compose 본문 builder 정정 — 인사말 + 제품 + Lot + note + SplitTable embed.
   6. tests/inform/test_attached_set_resolve.py 신규.
   7. tests/inform/test_inform_recipients.py 신규.
   8. tests/inform/test_inform_mail_body.py 신규 — body 에 KNOB prefix 없음, note 포함.

B) Frontend (My_Inform.jsx)
   1. 카드/detail 첨부 stack 렌더 — set 이름 아래에 SplitTable HTML 임베드.
   2. SplitTable 좌상단 cell 빌더 — 컬럼 이름만 (값 join 제거).
   3. wizard 메일 미리보기 left panel:
      - "KNOB 강조" 영역 제거.
      - "자동 수신자" → "📨 수신자" 영역 (그룹 + 개별 multi-select).
   4. wizard 본문 빌더 — KNOB prefix 등 메타 제거. 인사말 + 제품 + Lot + note.
   5. 풀화면 모드 toggle — 미리보기 영역 폭 80vw.
   6. 미리보기 SplitTable 폭 cleanup (font 11px / padding / table-layout fixed).

C) 회귀 (반드시 유지)
   - task #1~18 모두 회귀 X.
   - 특히 task #14 의 표 모드/wizard 5단계/매트릭스/디테일 풀폭/모듈색 row.
   - task #15 의 dashboard 인폼 차트.
   - task #16~18 의 agent capability/시나리오/채팅 UX.
   - 메일 throttle, 댓글, 첨부 history.

D) 빌드
   - python3 _build_setup.py 재빌드
   - npm --prefix frontend run build

E) 테스트
   - 위 신규 + 기존 회귀.
   - smoke (scripts/smoke_test.py) 통과.

[제약]
- 페이지 신설 X.
- backend endpoint 시그니처 호환 (필드 추가만).
- admin_settings.json 절대 수정 X, 내용 출력 X.
- git add/commit/push 절대 X.
- task #1~18 회귀 X.

[완료 기준]
- 저장된 커스텀 세트 첨부 시 detail 에 SplitTable HTML preview 표시.
- SplitTable 좌상단 cell 에 컬럼 이름만 (값 미표시).
- 메일 미리보기에 KNOB 강조 panel 제거.
- 자동 수신자 → 그룹 + 개별 수신자 multi-select.
- 메일 본문 = 인사말 + 제품 + Lot + note (메타 prefix 없음).
- 풀화면 모드에서 SplitTable 25 wafer 풀폭 표시.
- 신규 테스트 + 기존 회귀 통과.
- frontend build + setup.py 재빌드 통과.
- 마지막에 'DISCORD_SUMMARY:' 한국어 3~5줄 요약.

[CHAIN — task #19 완료 후 자동 이어가기 (spark)]
이 task #19 가 끝났으면 (DISCORD_SUMMARY 출력 직전 또는 직후) 추가로:
1. `.codex_task_20_agent_gemini_integration_spec.txt` 파일을 read 도구로 전체 read.
2. spec 안 모든 요구사항 (Vertex AI Gemini 통합 + multi-provider 분기 + 모델 선택 UI + 비용 추적/budget cap + 사용량 모니터 + setup 가이드) 단계별로 빠짐없이 구현.
3. task #20 의 회귀/빌드/테스트 까지 마무리.
4. task #20 의 'DISCORD_SUMMARY:' 한국어 3~5줄도 출력 (task #19 요약과는 별도 블록).
같은 동일 제약 (git add/commit/push X, admin_settings.json X, OpenAI API X, agent 외 GCP X) 모두 task #20 에도 적용.
