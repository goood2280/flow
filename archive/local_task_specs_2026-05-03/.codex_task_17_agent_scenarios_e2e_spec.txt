flow 앱(/mnt/d/TEST_Making_Video/semi_all/flow) 에이전트의 파일탐색기/SplitTable/인폼로그 시나리오 30+ 자동 테스트 + multi-turn clarification 강화. task #16 (capability allowlist 제거 + tool registry) 위에 얹는 검증/보강 작업.

[배경 — 사용자 직언]
"에이전트로 파일탐색기 하는 일(SQL식으로 여러 항목 불러오기 등), SplitTable 랏 검색·특정 커스텀셋 불러오기 등, 인폼로그 여러 인폼 불러오기 등, 에이전트가 할 수 있는 시나리오 수십 가지 이상 테스트해줘. 모든 걸 완벽하게 적지 않고 일부만 적을 때도 관련 질문들을 주면서 결론지을 수 있도록 확인해줘."

해석:
- task #16 의 tool registry 가 실제로 동작하는지 30+ 시나리오로 검증.
- 사용자가 모호하게 말해도 agent 가 자연어로 명확화 질문 던지고 답을 받아 마무리.
- 시나리오 카탈로그를 사용자가 화면에서 클릭해서 따라할 수 있도록.

[원하는 결과]

1) Agent multi-turn clarification (slot filling).
   - 누락 인자 자동 감지 → 한국어 자연어 question 생성 ("어떤 제품의 랏을 찾으세요? — PROD-A / PROD-B / ...").
   - 사용자 답변 → 같은 대화 컨텍스트 안에서 인자 채움 → tool 호출.
   - 모호한 동사 ("보여줘" / "확인" / "정리") → 현재 페이지 컨텍스트 또는 질문.
   - 권한 거부 / 데이터 없음 → 한국어 안내.
   - 시스템 prompt 에 slot filling 패턴 5+ 명시 (한국어 예시 포함).

2) 시나리오 카탈로그 30+.
   각 시나리오 = {id, category, title, prompt_user, persona, expected_clarification (옵션), expected_tool_calls, expected_keywords_in_response}.
   - 일부 시나리오는 single-turn (모든 인자 명시).
   - 일부 시나리오는 multi-turn (인자 누락 → clarification → 답변 → tool).

   **A. 파일탐색기 (10)**
   1. "파일 목록 보여줘" → clarification: "어떤 폴더?" → "/data/Fab" → /api/files/list
   2. "/config/work/sharedworkspace/Base 안 ML_TABLE 파일 다 보여줘" → /api/files/list (single-turn)
   3. "ML_TABLE_PRODA 컬럼 뭐 있어" → /api/files/preview 또는 /api/splittable/ml-table-cols
   4. "SQL: SELECT * FROM ML_TABLE_PRODA WHERE wafer_id < 5" → /api/files/sql-preview (apply_sql_like)
   5. "어제 업로드한 parquet 파일" → /api/files/list + date filter
   6. "data 폴더 트리 다 보여줘" → /api/files/tree
   7. "이 파일 미리보기" → clarification: "어떤 파일 경로?" → 답변 → preview
   8. "knob 컬럼이 있는 파일 찾아" → /api/files/search keyword=knob
   9. "PROD-A hive view 열어줘" → /api/files/hive-view
   10. "100MB 이상인 parquet 파일 list" → /api/files/list + size filter

   **B. SplitTable (10)**
   1. "PROD-A 랏 보여줘" → /api/splittable/products + /lot-ids
   2. "lot ABC12 view 보여줘" → /api/splittable/lot-candidates?root=ABC12
   3. "knob 컬럼만 추가해서 view" → /api/splittable/customs/save (custom_cols=KNOB)
   4. "내 커스텀 세트 보여줘" → /api/splittable/paste-sets 또는 /splittable-sets (task #14 통합)
   5. "이 세트 저장해줘" → clarification: "세트 이름?" → 답변 → /api/splittable/paste-sets/save
   6. "FAB long format ML_TABLE 가 뭐 있어" → /api/splittable/long-items
   7. "ML_TABLE_PRODA 매칭 디렉토리" → /api/splittable/ml-table-match
   8. "이번 plan 노트로 저장해줘" → clarification: "lot? plan 내용?" → /api/splittable/notes/save
   9. "override 디버그 — lot ABC123" → /api/splittable/override-debug
   10. "fab_lot 25개 잘라서 view" → clarification: "어느 root_lot?" → 답변 → lot-candidates + custom

   **C. 인폼로그 (10+)**
   1. "이번주 인폼 보여줘" → /api/informs?period=7d
   2. "PROD-A의 GATE 인폼 다 보여줘" → /api/informs?product=PROD-A&module=GATE
   3. "lot ABC123 진행률" → /api/informs/by-lot?lot_id=ABC123
   4. "어제 미해결 인폼" → /api/informs?status=pending + date filter
   5. "신규 인폼 작성" → multi-turn: 제품 → root_lot → fab_lot → 모듈 → 본문 → 첨부 → POST /api/informs
   6. "이 인폼 완료 처리" → clarification: "어떤 inform_id?" → POST /api/informs/status
   7. "GATE 모듈 차트 대시보드에 추가" → /api/informs/dashboard-data preset 추가 (task #15)
   8. "audit log 최근 50개 어제부터" → /api/informs/audit-log
   9. "내가 받은 인폼" → /api/informs?my=true
   10. "인폼 매트릭스 PROD-A 만" → /api/informs/lot-matrix?product=PROD-A
   11. "어제 GATE 모듈에 인폼한 사람 누구" → audit-log + filter
   12. "lot ABC123 인폼 검토 후 메일 재발송" → multi-turn: 인폼 선택 → POST /api/informs/{id}/mail/resend

3) 자동 테스트 framework.
   - tests/agent/test_agent_e2e_scenarios.py 신규.
   - SCENARIOS = [...] fixture (위 30+).
   - parametrize 로 모든 시나리오 1회 실행.
   - 각 시나리오: agent endpoint (/api/agent/chat 또는 /api/llm/agent/run) mock 호출 → 응답 검증.
   - 검증 항목:
     * expected_clarification 이 있으면 응답에 question 포함 확인 → follow-up 답변 시뮬레이션 → 후속 호출 검증
     * expected_tool_calls — 호출된 tool 이름 / 핵심 인자 존재
     * expected_keywords_in_response — 한국어 결과 요약에 키워드 포함
   - 테스트 결과 30/30 통과 목표 (실패 케이스는 reason 표시).

4) 시나리오 카탈로그 endpoint.
   - GET /api/agent/scenarios?category= → 카탈로그 list.
   - 응답: {category: [{id, title, prompt, multi_turn, persona}]}.
   - in-memory cache (changes 시 재로드).

5) 시나리오 카탈로그 UI (frontend).
   - My_Diagnosis.jsx 에 "💡 예시 시나리오" 카테고리 추가 (CATEGORIES 끝에 신설 또는 기존 카테고리 안에).
   - 3 sub-tab: 파일탐색기 / SplitTable / 인폼.
   - 각 카드: 제목 + prompt 1줄 + 카테고리 배지 + (multi-turn 이면 🔁 아이콘).
   - 카드 클릭 → 에이전트 채팅창에 prompt 자동 입력 (전송 X — 사용자가 변형 후 보냄).
   - 검색 input (시나리오 제목/prompt substring filter).

[코드 reference]
- backend/routers/llm.py — agent chat endpoint, system prompt
- backend/routers/files.py / filebrowser.py / splittable.py / informs.py — tool 호출 대상
- frontend/src/pages/My_Diagnosis.jsx — 카탈로그 UI

[작업 순서]

A) Backend
   1. agent system prompt 에 slot filling 규칙 + 한국어 예시 5+ 추가.
   2. /api/agent/scenarios 신규 — 30+ 카탈로그 list.
   3. multi-turn clarification 핸들러: 누락 인자 자동 감지 → question wrap.
   4. tests/agent/test_agent_e2e_scenarios.py 신규 — 30+ parametrize.
   5. tests/agent/test_agent_clarification.py 신규 — slot filling 단위 테스트.

B) Frontend
   1. My_Diagnosis.jsx 에 "💡 예시 시나리오" 카테고리 추가.
   2. 3 sub-tab + 카드 grid + 검색.
   3. 카드 클릭 → 에이전트 입력창에 prompt 자동 채움.

C) 회귀 (반드시 유지)
   - task #1~16 모두 회귀 X.
   - 특히 task #16 의 capability list 제거 + RBAC + tool registry 그대로 활용.

D) 빌드
   - python3 _build_setup.py 재빌드
   - npm --prefix frontend run build

E) 테스트
   - 위 신규 + 기존 회귀.
   - smoke (scripts/smoke_test.py) 통과.
   - 30+ 시나리오 모두 PASS 확인 (실패 시 reason 명시).

[제약]
- 페이지 신설 X.
- backend endpoint 시그니처 호환 (신규만).
- admin_settings.json 절대 수정 X, 내용 출력 X.
- git add/commit/push 절대 X.
- task #1~16 회귀 X.
- 시나리오 테스트는 mock 또는 실제 fixture 데이터 사용 — admin_settings 의존 X.

[완료 기준]
- 30+ 시나리오 카탈로그 endpoint + 자동 테스트 30+ 통과.
- agent multi-turn clarification 5+ 케이스 통과.
- agent 페이지 "💡 예시 시나리오" 카테고리 + 3 sub-tab + 카드 grid.
- frontend build + setup.py 재빌드 통과.
- 마지막에 'DISCORD_SUMMARY:' 한국어 3~5줄 요약.

[CHAIN — task #17 완료 후 자동 이어가기]
이 task #17 가 끝났으면 (DISCORD_SUMMARY 출력 직전 또는 직후) 추가로:
1. `.codex_task_18_agent_rich_chat_ux_spec.txt` 파일을 read 도구로 전체 read.
2. spec 안 모든 요구사항 (응답 schema 확장 + thread 영속화 + Claude 같은 채팅 UI + streaming + 컨텍스트 자동 누적 + suggestions/follow_up_inputs 하단 패널 + markdown/table/code 인라인 렌더) 단계별로 빠짐없이 구현.
3. task #18 의 회귀/빌드/테스트 까지 마무리.
4. task #18 의 'DISCORD_SUMMARY:' 한국어 3~5줄도 출력 (task #17 요약과는 별도 블록).
같은 동일 제약 (git add/commit/push X, admin_settings.json X, 내용 출력 X) 모두 task #18 에도 적용.
