flow 앱(/mnt/d/TEST_Making_Video/semi_all/flow)의 홈 Flowi 의 missing 필드 보완 흐름 끝까지 이어지지 않는 버그 fix.

[현 문제]
- 사용자가 "A1001 인폼로그 남겨줘" 입력 → Flowi 응답 missing=["인폼 내용"] (자유 텍스트 필드).
- 응답 카드 하단 button 으로 "1 필수값 이어서 입력 — 부족한 값(인폼 내용)을 추가해 같은 등록 요청을 이어갑니다" 만 보임.
- 그 button 클릭 시 실제 입력 textbox 가 노출되지 않고 ask 도 의미 있게 이어지지 않음 → 진행 불가.

[원인 추정]
- arguments_choices 가 자유 텍스트 missing 에서도 안내 button 한 개를 채워 보내는 구조.
- onChoice 가 그 안내 텍스트("필수값 이어서 입력")를 새 prompt 로 ask() 호출 → backend 가 그걸 사용자 입력으로 인식하지 않아 다시 missing.
- conversation 상태 유지(이전 prompt + 새 입력 결합) 없음.

[원하는 결과]
- missing 필드가 자유 텍스트(note/reason/사유/내용/메모 등)인 경우 button 안내 X. 대신 inline textbox 자동 노출 + placeholder 친근 안내 + 보내기 버튼 + Enter 전송.
- 텍스트 입력 후 전송 시 이전 사용자 prompt + 새 입력을 자동 결합해 ask() 호출(예: "A1001 인폼로그 남겨줘" + " 내용은 GATE 모듈 인폼입니다." → 한 번에 register_inform_log 전체 인자 채움).
- missing 필드가 후보 값이 명확한 경우(product/module/source_type/step 등 카탈로그 list 가능)는 기존처럼 1/2/3 button + free-input 동시 노출.
- admin/일반 동일 UX.

[현 코드 reference]
- frontend/src/pages/My_Home.jsx
  * FlowiResult, ask(), onChoice 흐름. arguments_choices 렌더 부분(task #9 가 button 으로 변경).
- backend/routers/llm.py
  * _structure_flowi_function_call() 의 arguments_choices 생성 부분(task #2 spec E 항목).
  * /api/llm/flowi/chat 응답 shape.
  * 이전 prompt 합치기 helper (있으면). task #2 의 walkthrough 세션과 별개로, 단일 함수 호출 missing 보완 path.

[작업]

A) Backend — missing 분류 + arguments_choices 정돈
   ★ backend/routers/llm.py 의 _structure_flowi_function_call().
   - missing 필드 키 분류:
     * 자유 텍스트형: note, reason, 사유, 내용, memo, comment, knob_value, keyword, title, description (확장 가능 set)
     * 카탈로그형: product, module, source_type, step, metric, root_lot_ids, fab_lot_ids, lot_ids, wafer_ids, split_set 등
   - 응답 shape 변경:
     * arguments_choices 는 카탈로그형 missing 에 대해서만 [{id,label,value}] 채움.
     * 자유 텍스트형 missing 은 arguments_choices 비우고 missing_freetext: [{key, label, placeholder}] 새 키에 채움.
     * 두 키 모두 응답에 동봉(둘 다 비어있으면 안 보임).
   - "필수값 이어서 입력" 같은 메타 안내 button 은 절대 만들지 마. 그건 UI 안내 텍스트로만.

B) Backend — 이전 prompt 결합용 conversation 키
   ★ /api/llm/flowi/chat 응답에 last_partial_prompt 키 추가.
   - last_partial_prompt = 현재 사용자 prompt 그대로(missing 보완 시 FE 가 다음 ask 에서 그 앞에 prepend).
   - 또는 더 간단: arguments_partial 키 — 이번 라우팅에서 채운 arguments JSON. FE 가 다음 ask 시 그걸 system context 로 동봉.
   - 가장 단순한 path 채택: FE 가 arguments_partial + 사용자 새 입력을 자동 prompt 결합("<previous_prompt> <missing_label>: <new_input>"). Backend 변경 최소.

C) Frontend — FlowiResult 렌더 분기
   ★ frontend/src/pages/My_Home.jsx FlowiResult.
   - result.arguments_choices 가 비어있지 않으면: 기존 task #9 button list (각 missing key 별 sub-section + button + free-input).
   - result.missing_freetext 가 비어있지 않으면: 각 항목별 inline textbox 자동 노출. label + placeholder. 보내기 버튼 + Enter 전송. Esc 시 입력 취소.
   - 두 가지 동시 노출 가능. 하나의 missing 필드가 양쪽에 같이 들어가지는 않게(자유 텍스트 우선).
   - textbox 전송 시: ask(`${last_user_prompt} ${missing_freetext.label}: ${input_value}`) 으로 자동 prompt 결합. 사용자가 같은 흐름으로 이어가는 느낌.
   - admin/일반 동일.

D) 회귀 케이스 (반드시 유지)
   - Q1~Q8 자연어 라우팅 + walkthrough 세션 (task #2)
   - chart_session_id multi-turn (task #5)
   - 카탈로그형 missing button + free-input (task #9)
   - 인라인 응답 (table/chart/lot_list/split_view) (task #9)
   - 권한 매트릭스 (task #4)
   - 페르소나/금지룰 카드 (task #2)

E) 시연 케이스 (테스트로 검증)
   1. "A1001 인폼로그 남겨줘" → missing_freetext=[{key:"note", label:"인폼 내용", placeholder:"메모를 적어주세요(예: GATE 모듈 인폼)"}], arguments_choices=[].
   2. 사용자가 textbox 에 "GATE 모듈 인폼입니다." 입력 + 보내기 → ask("A1001 인폼로그 남겨줘 인폼 내용: GATE 모듈 인폼입니다.") → register_inform_log 가 product 도 missing 일 수 있음. 이번엔 카탈로그형(product) → arguments_choices 채움.
   3. 사용자가 product button 클릭(예: "PRODA 로 진행") → ask("A1001 인폼로그 남겨줘 인폼 내용: GATE 모듈 인폼입니다. PRODA") → 모든 인자 채워져 dry-run preview 응답.
   4. 사용자 confirm → 실제 등록.

F) 테스트
   - tests/test_flowi_router.py 보강:
     * "A1001 인폼로그 남겨줘" → missing_freetext 안에 note(또는 인폼 내용) 키 포함, arguments_choices 안 채워짐.
     * 자유 텍스트 missing 에 "필수값 이어서 입력" 같은 안내 button 이 arguments_choices 에 안 채워지는지.
     * note/reason/keyword 등 자유 텍스트 키 분류 정확.
     * product/module/source_type/step 등 카탈로그 키는 arguments_choices 그대로.
   - 기존 회귀 X.

G) 빌드
   - python3 _build_setup.py 재빌드.
   - npm --prefix frontend run build.

[제약]
- 기존 endpoint 시그니처 유지(응답에 키 추가만).
- admin_settings.json 절대 수정/내용 출력 X.
- git add/commit/push 절대 X.
- task #1~9 회귀 X.

[완료 기준]
- 자유 텍스트 missing 에서 inline textbox 자동 노출 + Enter/보내기 + 이전 prompt 결합 ask.
- 카탈로그 missing 은 기존 button + free-input.
- "A1001 인폼로그 남겨줘" 시연 케이스 정상 흐름(2~3 turn 안에 등록 dry-run preview 도달).
- pytest test_flowi_router.py 회귀 + 신규 통과.
- frontend build + setup.py 재빌드 통과.
- git status 의도된 변경만.
- 마지막에 'DISCORD_SUMMARY:' 한국어 3~5줄 요약.
