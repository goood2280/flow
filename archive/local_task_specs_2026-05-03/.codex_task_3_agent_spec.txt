flow 앱(/mnt/d/TEST_Making_Video/semi_all/flow)의 에이전트 탭(My_Diagnosis.jsx, 1010 라인)을 산만함을 제거하고 8 카테고리로 깔끔하게 재구성해. 페이지 신설 X — 기존 페이지의 탭 구조와 backend 라우터를 정리·확장.

[현 상태]
- App.jsx:68 에서 "에이전트" 라벨이 diagnosis 탭에 매핑됨.
- My_Diagnosis.jsx 에 TabStrip 으로 6 탭 — "Intent / slot 해석", "Source profile 확인", "Item 의미 해석", "RCA 지식 검색", "Whitelisted data tool", "Agent trace / 출력".
- LlmCfgPanel + FlowiQualityPanel 을 My_Admin.jsx 에서 import 해서 같이 표시.
- backend: _flowi_agent_guide_md, FLOWI_AGENT_GUIDE_FILE(flowi_agent_entrypoints.md), FLOWI_AGENT_FEATURE_GUIDE_DIR, /api/llm/flowi/agent/chat, /flowi/feedback*, /flowi/profile 일부 존재.
- task #2 (홈 Flowi 강화, commit 7805b21) + task #5 (Dashboard, commit 92f16f1) 가 이미 main 에 들어가 있음. promoted_knowledge → 홈 Flowi system prompt 자동 append 흐름은 이번 task 에서 추가.

[목표 — 8 카테고리]
좌측 nav 8 카테고리, 우측 단일 패널(선택 카테고리만 노출). admin 카테고리는 admin 에게만 보이고 일반 유저는 잠금 표시 또는 hide.

1) 워크플로우
   - Flowi pipeline stage 다이어그램(텍스트 카드 chain): 입력 prompt → slot extract(rule) → intent infer(rule) → arguments 정형화 → action 실행/dispatch → polish(LLM, 선택) → 응답.
   - 각 stage 카드에 사용 모듈/함수/도메인지식 출처 표시. task #2 의 10 함수 + task #5 의 cross-DB join 흐름 표시.
   - 신규 GET /api/agent/workflow → stage 정의 dump.

2) 유저 페르소나
   - 현재 로그인 유저의 활동/관심 요약: 최근 사용 모듈, 자주 본 product/lot, 선호 출력 방식.
   - 도메인 관리자/Admin 페르소나 정의 카드(앱이 가정하는 행동 모델).
   - "도우미가 도와주는 일 / 하지 않는 일" 카드 — task #2 에서 만든 GET /api/llm/flowi/persona-card 응답 그대로 재사용. do_list/dont_list 두 칼럼 깔끔히. 사용자 적응 시간을 줄이기 위해 기본 펼침.
   - 신규 GET /api/agent/persona → {role, recent_modules, frequent_products, last_actions[], style_hints} (사용자 본인만 자기 데이터).

3) 프롬프트 해석 (live demo)
   - 기존 "Intent / slot 해석"을 여기로 통합.
   - 입력창 + 실행 버튼 → 결과 카드: 추출된 slots(product/root_lot/fab_lot/wafer/step/knob), selected_function, arguments JSON, 사용된 few-shot 예시(존재 시), missing 필드 → 1/2/3 choices.
   - 기존 SAMPLE_PROMPT/FUNCTION_TEST_PROMPT 버튼은 카테고리 안 quick action 으로 유지.
   - task #2 의 8 acceptance 패턴(Q1~Q8) 모두 미리 quick prompt 로 등록.

4) 지식 인벤토리
   - 기존 "RCA 지식 검색" 을 여기로 통합 + 확장.
   - 분류된 카드 list: knowledge_cards / causal_edges / similar_cases / custom_knowledge / agent_features / promoted_docs(에이전트 ingest 결과).
   - 검색창 + tag 필터 + 카테고리 토글.
   - 카드 클릭 → 우측 detail 패널에 본문/근거/관련 함수.
   - 신규 GET /api/agent/knowledge-inventory?q&tag&kind → 통합 리스트.
   - admin 은 카드 우상단 ★ 버튼으로 promote/unpromote 토글 → promoted 항목들의 title+요약(앞 200자)이 홈 Flowi 및 에이전트 system prompt 끝에 "[사내 지식]" 섹션으로 자동 append.

5) 최근 RAG 반영
   - 기존 "Agent trace / 출력" 을 여기로 정돈.
   - 최근 N(50)건의 Flowi 호출 trace: prompt, selected_function, retrieved knowledge id 리스트, score, 사용 시간, 결과 분류(성공/missing/오류).
   - 신규 GET /api/agent/recent-rag?limit&user → trace list. data/flow-data/flowi_activity.jsonl 기반 + retrieved_ids 필드 새로 기록.
   - polish 호출 시 어떤 knowledge 가 system prompt 에 들어갔는지 trace 에 기록.

6) Item 해석
   - 기존 "Item 의미 해석" 을 여기로 정돈.
   - semiconductor_knowledge 기반 item → knob/mask/step 변환 룰 시각화: 표(item, 매칭 step_id, 매칭 knob, 매칭 mask, 출처).
   - 검색 + 필터(source_type, product).
   - 신규 GET /api/agent/item-rules → semi_knowledge dump 요약.

7) LLM 설정
   - LlmCfgPanel 을 여기에 한 카테고리로 단독 배치. 산만하게 다른 섹션과 섞지 마.
   - admin token 은 마스킹된 상태로 표시(원래 그렇지만 한 번 더 확인).
   - 모델/엔드포인트/timeout/auth_mode/provider 빠르게 확인 + admin 은 inline 수정 + "테스트" 버튼.
   - 5종 provider 프리셋(openai / openai_compatible / local=GPT-OSS-120B / generic / playground) 토글 그대로 유지.

8) 관리 도구 (admin only)
   - 산만함 제거: 기존 "Whitelisted data tool" + RCA 입력 폼(table 폼)을 여기 admin 영역 안으로 이동. 일반 유저는 보이지 않는다.
   - 3 sub-section:
     a) 매칭 어시스턴트 — product 선택 + source_table 입력 + "추천" → ML_TABLE 컬럼 매핑 후보 표 + reason + 적용 버튼. backup 후 적용. (신규 POST /api/agent/admin-tools/matching/suggest|apply)
     b) 룰북 어시스턴트 — knob/mask + 변경 요약 → 영향 step/item 후보 + 적용 버튼. backup 후 적용. (신규 POST /api/agent/admin-tools/rulebook/suggest|apply)
     c) 지식 주입 — 제목/태그/타입 + 본문 textarea 또는 파일 업로드(.md/.csv/.pdf). chunk(1500자±200) 분할 저장. (신규 POST /api/agent/admin-tools/knowledge/ingest, GET /api/agent/admin-tools/knowledge/list)
   - 매칭/룰북 변경 backup: data/flow-data/agent_backups/<yyyymmdd_hhmmss>_<kind>.json. .gitignore 에 data/flow-data/agent_backups/ + data/flow-data/knowledge/raw/ 추가.

[UI 원칙]
- 좌측 8 카테고리 nav(icon + 라벨, admin 8번은 자물쇠 아이콘 + admin 만 클릭 가능).
- 우측 단일 패널, 선택 카테고리만 보임. 한 카테고리 안에서 산만하게 여러 폼 한꺼번에 띄우지 마.
- 카테고리 진입 시 상단에 한 줄 설명(이 카테고리가 무엇을 보여주는지).
- 빈 상태(데이터 없음)일 때 한국어 안내 문구.
- 페이지 폭: PageShell 그대로 유지. 스크롤 1depth.

[제약]
- 페이지 신설 금지. My_Diagnosis.jsx 안에서 재구성.
- 홈 Flowi(My_Home.jsx) 코드는 promoted_knowledge 자동 append 분기 외에는 건드리지 마(task #2 영역 보호).
- admin_settings.json 절대 수정/내용 출력 X.
- git add/commit/push 절대 X.
- 기존 6 탭이 가지고 있던 데이터/상태 마이그레이션 — 사용자가 새 카테고리 진입 시 동일 데이터 보이도록(기능 회귀 X).

[테스트]
- tests/agent/test_agent_endpoints.py 신규 — workflow/persona/recent-rag/knowledge-inventory/item-rules/admin-tools 권한과 응답 shape.
- 기존 RCA / Agent trace 테스트 회귀 없게.

[빌드]
- python3 _build_setup.py 재빌드.
- npm --prefix frontend run build.

[완료 기준]
- 8 카테고리 nav + 단일 패널 + admin 잠금, 7 backend GET 신규, 3 admin-tools 그룹, 산만한 RCA 폼/Whitelisted tool admin 영역 이동, .gitignore 갱신, 테스트 통과, frontend build 통과, setup.py 재빌드, git status 의도된 변경만.
- 마지막에 'DISCORD_SUMMARY:' 한국어 3~5줄 요약.
