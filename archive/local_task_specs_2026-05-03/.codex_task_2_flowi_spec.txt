flow 앱(/mnt/d/TEST_Making_Video/semi_all/flow)의 홈 Flowi 에이전트(My_Home.jsx의 FlowiConsole + /api/llm/flowi/chat)를 GPT-OSS-120B 같은 사내 오픈소스 LLM으로도 정확히 동작하도록 강화하고, 도메인 관리자가 자연어로 다음 6개 패턴을 정확히 처리하도록 함수 카탈로그/naming rule/few-shot/누락 보완 흐름을 모두 보강해.

[페르소나 — 홈 Flowi 한정]
- 사용자: lot 관리팀, 모듈팀(GATE/STI/PC/MOL/BEOL/ET/EDS/S-D Epi/Spacer/Well 등) 도메인 전문가. IT 적응 시간 0.
- 모델: GPT-OSS-120B 또는 동급 오픈소스. native function calling 약함 → JSON-mode + 검증 retry.
- 홈 Flowi에 admin 전용 함수(매칭테이블/룰북/Knowledge ingest)는 절대 노출 X. 그건 별도 에이전트 탭.

[반드시 처리해야 하는 8개 질문 패턴 — acceptance criteria]
Q1) "PRODA A1002 24.0 SORT KNOB 구성이 어떻게돼?"
    → product=PRODA, root_lot_ids=["A1002"], step="24.0 SORT", group="KNOB"
    → query_lot_knobs_from_ml_table (기존, step 필터 보강)
Q2) "PRODA A1002 #1 24.0 SORT Split이 뭐야? 뭘로 진행했어?"
    → product=PRODA, root_lot_ids=["A1002"], wafer_ids=[1], step="24.0 SORT"
    → query_wafer_split_at_step (신규)
Q3) "24.0 SORT PPID_24_3인 자재 가장 빠른게 어디에 있어?"
    → step="24.0 SORT", knob_value="PPID_24_3", sort="earliest_progress"
    → find_lots_by_knob_value (신규)
Q4) "A1002A.1 어디에 있어?"
    → fab_lot_ids=["A1002A.1"]
    → query_fab_progress (기존, fab_lot_id 단독 입력 처리 보강)
Q5) "A1000 #20 16.0 VIA2 Avg 몇이야?"
    → root_lot_ids=["A1000"], wafer_ids=[20], step="16.0 VIA2", metric="VIA2 Avg", agg="avg"
    → query_metric_at_step (신규)
Q6) "PRODA A1000A.3 GATE 모듈 인폼해줘 test1 스플릿으로 선택해줘 내용은 GATE 모듈인폼입니다."
    → product=PRODA, fab_lot_ids=["A1000A.3"], module="GATE", split_set="test1", note="GATE 모듈인폼입니다."
    → register_inform_log + compose_inform_module_mail 결합 (인폼 신규 작성 dry-run + 모듈 메일 미리보기 동봉)
    → side_effect="confirm_before_write"
    → 신규 결합 함수 또는 register_inform_log 응답 안에 mail_preview 동봉
Q7) "A1003 GATE는 test1 STI는 test2 이런식으로 A1003에 대해서 인폼로그 다 만들어줘"
    → root_lot_ids=["A1003"], entries=[{module:"GATE", split_set:"test1"}, {module:"STI", split_set:"test2"}, ...]
    → register_inform_log 의 batch 모드 (한 root_lot_id 에 module 별 split 매핑 N개를 한 번에 dry-run 등록)
    → side_effect="confirm_before_write"
    → 자연어 파싱 패턴: "<module> 는/은 <split>" 반복. 사용자 confirm 후 N개 인폼 entry 일괄 POST.
    → 누락(예: 어떤 module 에 split 누락) 항목 있으면 missing 표시 + entries 별 arguments_choices 제공.
Q8) "A1004 인폼전체 작성해줘" → 에이전트가 모듈을 하나씩 물어가며 자유도 높은 multi-turn walkthrough
    → 진행 흐름 예시:
      U: "A1004 인폼전체 작성해줘"
      A: "GATE는 뭘로 할까요? (예: test1)"
      U: "test1로 해줘"
      A: "STI는?"
      U: "이건 일단 생략할게"
      A: "PC는?" ... 모듈 list 끝까지
      A: "다른 모듈도 추가할까요? (예: BEOL)" 또는 "이대로 등록할까요? (현재 N개 entry)"
      U: "BEOL도 할게"
      A: "BEOL은?"
      U: "test3"
      A: "이대로 등록할까요?"
      U: "응"
      A: 등록 완료.
    → 신규 함수 register_inform_walkthrough (stateful multi-turn)
    → side_effect="confirm_before_write" — 등록 직전 사용자 확인.
    → 자유도: 한 모듈 skip("생략/넘어가/안 해"), 모듈 점프("BEOL도 할게"), 한 모듈 다중 split("test1 그리고 test2"), free-input note("내용은 …"), 중간 종료("끝/그만/이대로 등록").

[현 코드 reference]
- backend/routers/llm.py
  * FLOWI_AGENT_PERSONA, FLOWI_NAMING_RULES, FLOWI_DEFAULT_SYSTEM_PROMPT, FLOWI_READ_ONLY_POLICY (line ~100)
  * _flowi_infer_function_call(prompt, slots) — rule-based 함수 라우터 (line ~1091)
  * _flowi_function_schema(name) — function description + required[] (line ~1169)
  * _structure_flowi_function_call(prompt, product, max_rows) — slots+arguments 정형화 (line ~1199)
  * slot helper들: _slot_summary, _classified_lot_tokens, _wafer_tokens, _metric_alias_hits, _flowi_knob_tokens, _flowi_parse_splittable_plan_assignments, _flowi_invalid_wafer_mentions, _flowi_source_type_tokens, _flowi_product_resolution
  * llm_adapter.complete(prompt, system) — LLM polish 호출
  * /api/llm/flowi/chat (line ~11227) 홈 진입점
- backend/core/llm_adapter.py — provider 분기 (openai / openai_compatible / local=GPT-OSS-120B / generic / playground)
- backend/routers/informs.py — 인폼 관련 API (modules/recipients, modules/knob-map, send-mail 등 task #1에서 추가됨)
- frontend/src/pages/My_Home.jsx — FlowiConsole, FlowiResult, ask(), activate()
- data/flow-data/admin_settings.json — READ ONLY. 절대 수정/내용 출력 X.

[작업]

A) 함수 카탈로그 확장 — 기존 6 함수 + 신규 4종 + 기존 2종 보강 = 총 10
   ★ 구현 위치: _flowi_infer_function_call 의 if 분기 + _flowi_function_schema dict + _structure_flowi_function_call 의 selected_name 분기.

   1) compose_inform_module_mail (도메인 관리자, 메일)
      - 자연어 keys: "메일","통보","알림","공지","보내","발송" + module 토큰
      - arguments: {product, root_lot_ids[], fab_lot_ids[]?, module, lot_count, sender_label?, reason?}
      - schema required: ["root_lot_ids 또는 fab_lot_ids","module"]
      - confidence 0.88, side_effect="confirm_before_write"
      - 응답에 task #1의 GET /api/informs/modules/recipients?module={module} 결과(가능 시) 포함, 호출 실패 시 빈 list graceful degrade
   2) register_inform_log (단일 + batch 모드)
      - keys: "인폼","등록","기록","남겨","올려". 단 "메일/통보/알림/공지/보내/발송" 동시 시 1)이 우선
      - 단일 모드 arguments: {product, root_lot_ids[]?, fab_lot_ids[]?, module:str?, reason:str?, note:str?, split_set:str?}
      - 단일 schema required: ["root_lot_ids 또는 fab_lot_ids"]. module/note/split_set optional
      - 배치 모드 arguments(Q7 처리): {product?, root_lot_ids:[str], entries:[{module:str, split_set:str?, note:str?, reason:str?}]}
      - 배치 자연어 패턴: "A1003 GATE는 test1 STI는 test2 ..." → root_lot_ids=["A1003"], entries=[{module:"GATE", split_set:"test1"},{module:"STI", split_set:"test2"}]
      - 배치 인식 키: prompt에 "<module>는/은 <split>" 형태가 2회 이상 반복 또는 "다 만들어"/"전부 등록"/"각각" 키워드.
      - 배치 schema required: ["root_lot_ids","entries"]. entries 의 각 항목에서 module 필수, split_set/note/reason optional.
      - confidence 단일 0.78 / 배치 0.84 (배치는 더 명시적이라 신뢰도 높음). side_effect="confirm_before_write".
      - Q6 처리(단일+메일): prompt에 "인폼해줘" + module + split_set("...스플릿으로 선택") + note("내용은 ...") 모두 있으면 mail_preview 도 함께 동봉(GET /api/informs/{tmp_id}/mail-preview 와 동등한 dry-run 응답).
      - Q7 처리(배치): entries 각각에 대해 dry-run preview 동봉 + 누락 항목 entries[i].missing 표시. 사용자 confirm 후 N개 일괄 POST 처리(/api/llm/flowi/inform/confirm 가 batch 도 받게 확장).
   3) preview_filebrowser_data
      - keys: "파일","preview","DB","row","보여줘","schema","최근","latest" + source_type
      - arguments: {source_type:"FAB"|"INLINE"|"ET"|"VM"|"EDS", product, root_lot_ids[]?, fab_lot_ids[]?, wafer_ids[]?, limit:int}
      - limit: prompt 의 "100행/최근 100/top 100" 정규식. 1..500. default 100.
      - schema required: ["source_type","product"]
      - read_only=True. confidence 0.75
   4) search_filebrowser_schema
      - keys: "컬럼","찾아","검색","어떤 column","있는지","schema"
      - arguments: {keyword, source_type?}
      - schema required: ["keyword"]
      - read_only=True. confidence 0.7
   5) query_wafer_split_at_step (신규, Q2)
      - keys: "split","진행","뭘로","뭐 했","적용된" + (root_lot_id|fab_lot_id) + wafer + step
      - 의도: 특정 wafer 가 그 step 에서 받은 KNOB+MASK 조합(=split) 조회
      - arguments: {product?, root_lot_ids[], fab_lot_ids[]?, wafer_ids[], step:str}
      - schema required: ["root_lot_ids 또는 fab_lot_ids","wafer_ids","step"]
      - read_only=True. confidence 0.85
   6) find_lots_by_knob_value (신규, Q3)
      - keys: "인 자재","가장 빠른","어디에 있","진행 중","받은 lot" + step + knob_value
      - 의도: 모든 lot 중 그 step 에 특정 KNOB 값이 적용된 lot/wafer 인버스 검색 + 가장 빠른 FAB 진행(=가장 늦은 step 처리한 lot)
      - arguments: {product?, step:str, knob_value:str, sort:"earliest_progress"|"latest_progress", limit:int(default 10)}
      - schema required: ["step","knob_value"]
      - read_only=True. confidence 0.85
      - 응답에 join 결과 (lot_id, wafer_id, current_step, tkout_time) list
   7) query_metric_at_step (신규, Q5)
      - keys: "avg","median","평균","중앙값","얼마","몇이야","측정값" + step + metric alias
      - 의도: lot+wafer+step 에서 ET/INLINE 측정값 집계
      - arguments: {product?, root_lot_ids[], fab_lot_ids[]?, wafer_ids[]?, step:str, metric:str, agg:"avg"|"median"(default)}
      - schema required: ["root_lot_ids 또는 fab_lot_ids","step","metric"]
      - read_only=True. confidence 0.83

   기존 함수 보강:
   8) query_lot_knobs_from_ml_table — step 필터 인자 추가 (arguments 에 step:str 추가, 없으면 모든 step). group:str?(KNOB|MASK|INLINE|VM 등) 인자도 추가.
   9) query_fab_progress — fab_lot_id 단독 입력 처리. arguments.fab_lot_ids 만 있어도 product 추론(fab_lot_id 의 prefix 5자 = root_lot_id, 그 root 가 어느 product 에 속하는지 조회) 또는 product 미지정 허용.

   10) register_inform_walkthrough (신규, Q8 — stateful multi-turn 인폼 walkthrough)
       - keys (start): "인폼전체","전체 작성","전부 작성","다 작성","통째로","모든 모듈"
       - keys (resolve): walkthrough 세션 활성 시 prompt 의 자유 응답 — split_set token, "생략/skip/넘어가/안 해", "<다른 모듈>도", "끝/그만/이대로 등록"
       - 단계별 arguments:
         * start: {root_lot_ids:[str], product?:str}. 응답: {session_id, current_module, entries:[], next_question:"<module>는 뭘로 할까요?", arguments_choices: split_set 후보}
         * resolve: {session_id, action:"set"|"skip"|"jump"|"add_split"|"set_note"|"finalize", value?:str, target_module?:str}. 응답: 다음 질문 또는 finalize dry-run.
         * finalize: {session_id, confirm:bool}. confirm=true 면 batch POST /api/informs/ 수행.
       - schema required (start): ["root_lot_ids"]
       - schema required (resolve): ["session_id"]
       - confidence 0.9 (start), 0.85 (resolve)
       - side_effect="confirm_before_write" 까지 stateful, finalize confirm 후 실제 write.
       - 세션 저장: data/flow-data/flowi_inform_sessions/<session_id>.json (TTL 1h, 만료 정리 cron 또는 lazy eviction).
       - 세션 shape: {session_id, username, root_lot_ids[], product, modules_remaining[], current_module, entries[], created_at, last_active_at}
       - admin_settings.inform_user_modules 의 union 모듈 set 을 모듈 list 로 사용. 사용자 흐름 중에 모듈 추가/skip 자유.
       - 신규 endpoint:
         * POST /api/llm/flowi/inform/walkthrough/start {root_lot_ids[], product?} → {session_id, ...}
         * POST /api/llm/flowi/inform/walkthrough/resolve {session_id, action, value?, target_module?} → {next_question 또는 finalize_preview}
         * POST /api/llm/flowi/inform/walkthrough/confirm {session_id, confirm} → batch insert 결과
       - FE: My_Home.jsx FlowiResult 가 walkthrough 응답을 받으면 세션 진행 상태 표시(현재 모듈/완료/남은 모듈/누적 entries) + 자유입력+arguments_choices.

   라우터 우선순위(higher first):
   is_current_fab_lot_prompt > preview_splittable_plan_update >
   query_metric_at_step (metric+step 둘 다 있을 때) >
   query_wafer_split_at_step (wafer+step+"split/진행" 있을 때) >
   find_lots_by_knob_value (step+knob_value 있고 lot 없을 때) >
   query_lot_knobs_from_ml_table (KNOB/MASK 키워드) >
   compose_inform_module_mail (메일/통보/알림 키워드) >
   register_inform_log (인폼/등록 키워드) >
   _contains_chart_intent → build_dashboard_metric_chart >
   query_fab_progress (FAB/현재/진행/어디 키워드) >
   preview_filebrowser_data > search_filebrowser_schema >
   기존 entries → route_flowi_feature.

B) Naming rule 보강 — func_step / 모듈 / metric / KNOB value / split_set
   ★ FLOWI_NAMING_RULES 와 slot helper 모두 업데이트.
   1) func_step alias (Q1,Q2,Q3,Q5 핵심): "<숫자>.<숫자> <대문자모듈>" 형태 (예: "24.0 SORT", "16.0 VIA2", "1.0 STI", "8.0 SD_EPI") → step="24.0 SORT" 그대로 캡처. 정규식 r"(\d+\.\d+)\s+([A-Z][A-Z0-9_/]*)"
   2) module token (compose_inform_module_mail, register_inform_log): admin_settings.inform_user_modules 모든 username의 module union + _MODULE_ALIAS dict.
      _MODULE_ALIAS = {
        "GATE":["게이트","gate"], "STI":["sti","sti모듈"], "PC":["pc","photoresist"],
        "MOL":["mol"], "BEOL":["beol","후공정"], "ET":["et","이티","측정"],
        "EDS":["eds"], "S-D Epi":["sd","sde","sd epi","에피"],
        "Spacer":["spacer","스페이서"], "Well":["well","웰"], "기타":["기타","other"]
      }
   3) metric alias (Q5): "avg","평균","mean"→"avg". "median","중앙값"→"median". "min","max","std"도. + ET/INLINE 항목 alias (예: "CD","LKG","VIA2 Avg").
   4) knob_value (Q3): "PPID_<digits>_<digits>" 형태. 또는 일반 토큰. arguments.knob_value 캡처.
   5) split_set (Q6): prompt 에 "<token> 스플릿(으)로 선택" 또는 "split=<token>" 형태 → split_set="<token>". 자유 문자열.
   6) note (Q6): prompt 에 "내용은 <text>" 또는 "사유 <text>" 형태 → note="<text>". 따옴표 안 본문 추출.
   7) wafer: 기존 #N/WFN/W N/N번 slot/N번장/N장 + 1~25 강제 유지.

C) JSON-mode 검증 layer (small model 정확도 보강)
   ★ backend/routers/llm.py 에 신규 helper _flowi_complete_json(messages, schema_dict, *, max_retries=1):
   1) system message 끝에 강제 문구: "Return only a single JSON object matching the schema. No prose, no code fences."
   2) llm_adapter.complete() 호출
   3) 응답 json.loads → schema 키 존재성/타입 validate → 실패 시 1회 retry (피드백: "이전 응답이 schema에 안 맞다. {missing/typeerror}. 정확히 다음 키만 있는 JSON 객체로 다시 응답해라: {keys}.")
   4) 두 번째도 실패면 None 반환 (caller graceful fallback)
   - 사용처: rule-based confidence < 0.5 일 때만 보조. 호출 결과는 selected_function.confidence 0.65 + reason 에 " (LLM polish)" suffix.

D) Few-shot examples
   ★ FLOWI_NAMING_RULES 다음에 FLOWI_FUNCTION_FEW_SHOTS list (각 항목 {function, prompt, arguments}).
   - 10 함수 각각 자연어→arguments JSON 예시 2~3건. Q1~Q6 표현 모두 포함.
   - 예:
     Q1: "PRODA A1002 24.0 SORT KNOB 구성이 어떻게돼?" →
        function="query_lot_knobs_from_ml_table",
        arguments={product:"PRODA", root_lot_ids:["A1002"], step:"24.0 SORT", group:"KNOB"}
     Q2: "PRODA A1002 #1 24.0 SORT Split이 뭐야" →
        function="query_wafer_split_at_step",
        arguments={product:"PRODA", root_lot_ids:["A1002"], wafer_ids:[1], step:"24.0 SORT"}
     Q3: "24.0 SORT PPID_24_3인 자재 가장 빠른게 어디에 있어?" →
        function="find_lots_by_knob_value",
        arguments={step:"24.0 SORT", knob_value:"PPID_24_3", sort:"earliest_progress"}
     Q4: "A1002A.1 어디에 있어?" →
        function="query_fab_progress",
        arguments={fab_lot_ids:["A1002A.1"]}
     Q5: "A1000 #20 16.0 VIA2 Avg 몇이야?" →
        function="query_metric_at_step",
        arguments={root_lot_ids:["A1000"], wafer_ids:[20], step:"16.0 VIA2", metric:"VIA2 Avg", agg:"avg"}
     Q6: "PRODA A1000A.3 GATE 모듈 인폼해줘 test1 스플릿으로 선택해줘 내용은 GATE 모듈인폼입니다." →
        function="register_inform_log",
        arguments={product:"PRODA", fab_lot_ids:["A1000A.3"], module:"GATE", split_set:"test1", note:"GATE 모듈인폼입니다."}
     Q7: "A1003 GATE는 test1 STI는 test2 이런식으로 다 만들어줘" →
        function="register_inform_log",
        arguments={root_lot_ids:["A1003"], entries:[{module:"GATE",split_set:"test1"},{module:"STI",split_set:"test2"}]}
     Q8 start: "A1004 인폼전체 작성해줘" →
        function="register_inform_walkthrough",
        arguments={root_lot_ids:["A1004"], action:"start"}
     Q8 resolve: (session active) "test1로 해줘" →
        function="register_inform_walkthrough",
        arguments={session_id:"<active>", action:"set", value:"test1"}
     Q8 skip: (session active) "이건 일단 생략할게" →
        function="register_inform_walkthrough",
        arguments={session_id:"<active>", action:"skip"}
     Q8 jump: (session active) "BEOL도 할게" →
        function="register_inform_walkthrough",
        arguments={session_id:"<active>", action:"jump", target_module:"BEOL"}
     compose_inform_module_mail:
        "GATE팀에 A1234 plan 적용 통보 메일" → {root_lot_ids:["A1234"], module:"GATE", lot_count:1}
   - few-shot은 LLM 호출 시 system message 에 [Few-shot examples] 섹션으로 append. rule-based 만 쓰는 호출에는 X.

E) Missing 필드 → 1/2/3 choices 자동 생성 (도메인 관리자 적응 0)
   ★ _structure_flowi_function_call() return 직전, missing 비어있지 않으면 arguments_choices 추가 + 자유 입력 안내.
   - 키별 룰:
     * "product" → product_config.list_products() 상위 3개 + "다른 제품 입력"
     * "root_lot_ids"/"lot_ids"/"fab_lot_ids" → 최근 본 lot 1~3개 (data/flow-data/flowi_users/<username>.md 또는 flowi_activity.jsonl 파싱) + "lot 직접 입력"
     * "module" → admin_settings.inform_user_modules union 상위 3개 + "다른 모듈 입력"
     * "step" → product 의 func_step 후보 상위 3개 + "step 직접 입력"
     * "metric" → ["DIBL","SS","CD","VIA2 Avg","LKG"] 중 prompt 가까운 3개 + "다른 항목 입력"
     * "knob_value" → 안내 placeholder + "값 직접 입력"
     * "source_type" → ["FAB","ET","INLINE","VM","EDS"] 5개 중 3개
     * "split_set" → 최근 SplitTable의 split set 이름 상위 3개 + "직접 입력"
     * "note" → "메모 직접 입력" placeholder
   - choices shape: [{id:"1",label:"PRODA로 진행",value:"PRODA"}, ...]
   - free-input 안내: arguments_choices 옆에 "또는 직접 입력해 주세요" 한 줄.
   ★ FE: My_Home.jsx FlowiResult 에 result.arguments_choices 있으면 missing 필드별 카드 list → 1/2/3 라디오 버튼 + free-input 텍스트박스. 사용자 선택/입력 시 prompt + " " + value(또는 입력값) 로 ask() 재호출. 기존 패널 톤 유지.

F) Q6 인폼 자연어 흐름 — 누락 보완 + dry-run 미리보기
   ★ register_inform_log 응답에 mail_preview 결합 (Q6 처럼 mail 까지 의도 포함 시).
   - prompt 에 "인폼" + module + (root_lot_id|fab_lot_id) 모두 있으면 register_inform_log dry-run + mail_preview 동봉.
   - 누락 항목별 선택지 → 위 E) arguments_choices 흐름 그대로.
   - 사용자 confirm 받기 전엔 실제 POST /api/informs/ 또는 send-mail 절대 X. side_effect="confirm_before_write" 강제.
   - confirm 응답 받은 뒤 (별도 endpoint /api/llm/flowi/inform/confirm 신규) 실제 POST 수행.

G) 응답 정리(도메인 관리자 친화)
   - selected_function.reason 80자 이내 한국어 한 줄.
   - rule-based confidence < 0.5 일 때만 _flowi_complete_json polish.
   - polish 실패(network/timeout/429) 시 rule-based 결과 + result.warnings 에 "LLM 응답이 늦어 기본 분석 결과만 표시했어요".
   - 일반 유저(role!="admin") 응답에서 intent/live_trace/context_used 등 디버그 필드 backend 단에서 redact.

H) 페르소나/금지룰 가시화 (홈 Flowi 콘솔)
   ★ FlowiConsole START 직전 또는 첫 진입 시 안내 카드(접고 펼치기, localStorage 세션당 1회 자동 펼침).
   - 카드 제목: "이 도우미가 도와주는 일 / 하지 않는 일"
   - 도와주는 일: "lot 조회·plan 등록/통보·인폼 메일·파일/DB preview·KNOB/MASK·FAB 진행·ET 측정·자연어 인폼 등록"
   - 하지 않는 일: FLOWI_READ_ONLY_POLICY + admin_settings.flowi_persona.must_not 줄단위 split → 4~5줄 친근 한국어
   - 데이터: 신규 GET /api/llm/flowi/persona-card → {persona, do_list[], dont_list[]}

I) 권한 거절 응답
   - /api/llm/flowi/chat 응답 후처리: admin 전용 함수(매칭/룰북/Knowledge ingest) 트리거 prompt 받으면 후보에서 그 함수 제거 + blocked=true + reject_reason "이 작업은 권한이 필요해요. 관리자에게 요청해 주세요."
   - blocked=true 응답엔 missing/arguments_choices 동봉 X.
   - HTTP 200 유지(클라이언트가 친근 응답 표시).

J) 회귀 케이스 (반드시 통과)
   - "PRODA A1000 #6 현재 fab lot id가 뭐야?" → query_current_fab_lot_from_fab_db
   - "PRODA A1000 A KNOB #1~10은 ABC로 plan" → preview_splittable_plan_update
   - "PRODA A1000 STI knob 알려줘" → query_lot_knobs_from_ml_table (기존)
   - "PRODA Inline CD scatter" → build_dashboard_metric_chart
   - "PRODA A1000 현재 step 어디" → query_fab_progress

K) Edge cases (테스트 명시)
   - module 없이 "메일 보내" → compose_inform_module_mail missing=["module"] + arguments_choices 모듈 후보 3개.
   - "A1002 #1 24.0 SORT Split" (product 미지정) → query_wafer_split_at_step missing=["product"] + arguments_choices product 후보 3개.
   - wafer "26번" → invalid_wafers=["26"] + warnings "26번 wafer는 유효하지 않아요. 1~25만 처리 가능."
   - step "24.0 SORT" alias 인식 — 영문 대문자 + 숫자.숫자 형태 정규식 통과.
   - Q6 처럼 split_set + note 모두 자연어 추출.

L) 실행 순서 (codex 단계별)
   1. backend/routers/llm.py 읽기 → 현 6 함수 분기/schema/return shape 파악.
   2. _MODULE_ALIAS, _flowi_module_token, _flowi_func_step_token, _flowi_metric_token, _flowi_knob_value_token, _flowi_split_set_token, _flowi_note_extract helper 추가.
   3. 새 함수 4종 schema 등록 (_flowi_function_schema). 기존 2종 schema 보강.
   4. _flowi_infer_function_call 에 4 분기 추가 + 우선순위 재정렬.
   5. _structure_flowi_function_call 에 신규 keys arguments + missing + arguments_choices + free-input 안내 처리.
   6. _flowi_complete_json helper + 사용처 (confidence<0.5).
   7. FLOWI_FUNCTION_FEW_SHOTS list + LLM 호출 system message 에 합류.
   8. /api/llm/flowi/persona-card 신규 endpoint.
   9. /api/llm/flowi/inform/confirm 신규 endpoint (Q6 confirm-after).
   10. /api/llm/flowi/chat 응답 후처리 (admin redact + blocked reject).
   11. FE My_Home.jsx FlowiResult arguments_choices/free-input 렌더 + 페르소나 카드.
   12. tests/test_flowi_router.py 에 Q1~Q6 + 회귀/edge case 추가.
   13. python3 _build_setup.py / npm --prefix frontend run build / pytest 검증.
   14. DISCORD_SUMMARY.

M) 빌드/검증
   - python3 _build_setup.py 재빌드.
   - npm --prefix frontend run build.
   - pytest tests/test_flowi_router.py -q 통과.
   - pytest tests 전체 회귀 X.

[제약]
- 홈 Flowi 에 매칭/룰북/Knowledge ingest 함수 추가 X (별도 에이전트 탭 task).
- git add/commit/push 절대 X.
- admin_settings.json 절대 수정 X, 내용 출력 X. 읽기는 OK 단 token/key 절대 출력 X.
- 기존 6 함수 시그니처/엔드포인트 회귀 X.
- llm_adapter.complete() 시그니처 유지 — 새 helper 로 감싸기.

[완료 기준]
- 신규 4 함수 + 기존 2 함수 보강 + JSON-mode helper + few-shot + missing→choices + free-input + Q6 dry-run+mail_preview + 페르소나 카드 + 권한 거절 모두 구현.
- Q1~Q6 자연어 라우팅 정확. 회귀/edge case 모두 통과. frontend build 통과. setup.py 재빌드. git status 의도된 변경만.
- 마지막에 'DISCORD_SUMMARY:' 헤더로 한국어 3~5줄 요약.
